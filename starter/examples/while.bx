def main() {
    var x = 1 : int;
    while (x == 1) {
        print(x);
        x = x - 1;
    }
    x = 4;
    while (x >= 0) {
        x = x - 1;
        print(x);
        if (x == 1){
            break;
        }
        continue;
        x = 2;
    }
    return;
}