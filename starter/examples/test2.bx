def main() {
    var x = 0 : int;
    var y = 0 : int;

    if (x == y){
        print(2);
        if (x == y){
            print(3);
        } else if (y == 0){
            print(4);
        }
    }

    if (x == 0){
        print(3);
        if (x > 2){
            print(1);
        } else {
            print(2);
        }
    }
    return;
}