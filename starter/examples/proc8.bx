def add(a : int, b : int) : int {
    return a + b;
}

def square(x : int) : int {
    return x * x;
}

def main() {
    var result = square(add(3, 4)) : int;
    print(result);  // Expected output
    return;
}
