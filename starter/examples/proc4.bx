def factorial(n : int) : int {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

def main() {
    var result = factorial(5) : int;
    print(result);  // Expected output
    return;
}
