def main() {
    var x = 1 : int;
    while (x) {
        print(x);
        x = x - 1;
    }
    x = 4;
    while (x) {
        x = x - 1;
        if (x == 1){
            break;
        }
        continue;
        x = 2;
    }
}